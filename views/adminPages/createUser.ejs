<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
    <title>Add User</title>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container">
                <a href="#" class="navbar-brand">Admin-panel</a>
                <a class="btn btn-dark" href="/adminLogout">Log Out</a>
            </div>
        </nav>
    </header>
    <div class="container px-5 mt-5 d-flex justify-content-center align-items-center">
        <div class="p-5 w-75 card shadow">
            <h1 class="text-center mb-4">Add User</h1>
            <form action="/add-user" method="post">
                <% if(userExist){ %>
                    <div class="m-3 py-1 rounded text-center border bg-danger-subtle">
                        <h3 class="text-danger">User already registered</h3>
                    </div>
                    <% } %>
                        <div class="mb-3">
                            <label for="exampleInputEmail1" class="form-label">Name</label>
                            <input type="text" name="name" class="form-control" id="exampleInputEmail1"
                                aria-describedby="emailHelp" />
                            <% if(inputErr){ %>
                                <span class="text-danger">
                                    <%= errMsg.name %>
                                </span>
                                <% } %>
                        </div>
                        <div class="mb-3">
                            <label for="exampleInputEmail1" class="form-label">Email address</label>
                            <input type="email" class="form-control" name="email" id="exampleInputEmail1"
                                aria-describedby="emailHelp" />
                            <% if(inputErr){ %>
                                <span class="text-danger">
                                    <%= errMsg.email %>
                                </span>
                                <% } %>
                        </div>
                        <div class="mb-3">
                            <label for="exampleInputEmail1" class="form-label">Phone</label>
                            <input type="number" name="phone" class="form-control" id="exampleInputEmail1"
                                aria-describedby="emailHelp" />
                            <% if(inputErr){ %>
                                <span class="text-danger">
                                    <%= errMsg.phone %>
                                </span>
                                <% } %>
                        </div>
                        <div class="mb-3">
                            <label for="exampleInputPassword1" class="form-label">Password</label>
                            <input type="password" name="password" class="form-control" id="exampleInputPassword1" />
                            <% if(inputErr){ %>
                                <span class="text-danger">
                                    <%= errMsg.password %>
                                </span>
                                <% } %>
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-dark w-25">Submit</button>
                        </div>
            </form>
        </div>
    </div>
</body>

</html>